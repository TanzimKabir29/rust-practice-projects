# file_organizer

A CLI that takes a directory path and prints its contents as tables — sorted by name, by size, and by last-modified date (ascending and descending).

Usage: `file_organizer <directory>`
